services:

  webapp:
    build: ./app
    image: europe-west1-docker.pkg.dev/responsible-map-435116-u9/webs/webapp:latest
    volumes:
      - ./app:/app
    networks:
      - web_network
    environment:
      - NAME=sergio
      - PORT=8080
    ports:
      - 80:8080

networks:
  web_network:
